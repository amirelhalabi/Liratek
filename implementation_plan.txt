Goal
Fix the web login error to allow browser testing, and then address UI issues in Exchange and Expenses components.

User Review Required
None safely mocked for web.
Proposed Changes
Authentication
[Modify] 
src/features/auth/context/AuthContext.tsx
Add a check for window.api existence.
If window.api is missing (web mode), use a mock login flow or bypass actual authentication check.
Exchange Component
[Modify] 
src/features/exchange/pages/Exchange/index.tsx
Remove max-h-[80vh] from the left column to allow it to match the height of the right column.
Expenses Component
[Modify] 
src/features/expenses/pages/Expenses/index.tsx
Check background colors.
If there is an inconsistency between the background and padding/gap, unify them.
The user mentioned "expense i see a different color in the background than the color of the padding". I will try to match the background of the container to the page or vice versa.
Verification Plan
Automated Tests
None.
Manual Verification
Open browser at http://localhost:5173.
Verify login works (bypassed or mocked).
Navigate to Exchange and check component heights.
Navigate to Expenses and check background colors.