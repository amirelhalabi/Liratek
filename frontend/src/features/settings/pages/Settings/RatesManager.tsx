// This component has been merged into CurrencyManager.tsx (ExchangeRatesSection).
// Kept as an empty file to preserve git history. Safe to delete.

export default function RatesManager() {
  return null;
}
