/**
 * Checkout-related constants
 */

/**
 * Payment tolerance for comparing amounts (to handle floating point precision)
 */
export const PAYMENT_TOLERANCE = 0.05;

/**
 * Prefix for receipt numbers
 */
export const RECEIPT_NUMBER_PREFIX = "RCP-";

/**
 * LBP rounding increment (smallest LBP bill denomination)
 */
export const LBP_ROUNDING_INCREMENT = 5000;

/**
 * Z-index for checkout modal
 */
export const CHECKOUT_MODAL_Z_INDEX = 50;

/**
 * Z-index for receipt preview modal
 */
export const RECEIPT_PREVIEW_Z_INDEX = 51;

/**
 * Default drawer name for sales
 */
export const DEFAULT_DRAWER_NAME = "DRAWER_B";
