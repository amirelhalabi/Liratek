# Handover — 2026-08-16 session end

**State: SAFE.** HEAD = `fb44b9ac` = tag `v1.30.2`, released, published, and
verified (CI all 5 jobs green including `Run Tests`; local jest core 195/1999 +
frontend 139/959+1 + backend 42/582; desktop e2e **255 passed, 7.9m**). Tree
clean, nothing uncommitted, no background work. better-sqlite3 was left on the
**Electron ABI** (desktop-e2e-ready).

Read `CLAUDE.md` and `MEMORY.md` first — this doc only covers what they don't:
this session's decisions, open threads, and fresh traps.

---

## 1. What shipped this session (all in v1.30.2)

| Commit | What | Why it matters |
| --- | --- | --- |
| `4424a70a` | Commission Settlement rework: column headers, **Top-up \| Other payment** toggle, cleaner summary | Top-up credits the provider drawer; Other payment renders the payment sheet into General. A double-post bug was caught pre-ship. |
| `7b076724` | Cash-flow arrows follow what actually moved (owner rule "b") | nothing debited → `in`; cash↔cash drawer → `both`; leaves business → `out`. `DRAWER_TOPUP` previously had NO badge case at all. |
| `0dc5e381` + `a9067416` | `lira-141-settlement-modes-and-topup-arrows.spec.ts` | Automates the owner's two manual checkpoints. **Read this spec before touching settlements/top-ups** — it captures real rendered values via `console.warn`. |
| `b92eea0b` | Settlement mode visible on the row | `counterparty.method` stamped the literal `"CASH"` on Top-up (no cash moved) → now `supplier.provider`. Row previously had NO payment-detail disclosure (leg stripped by `PROVIDER_STOCK_DRAWERS`); now shows `Top-up → Katsh drawer  861,369 LBP` / `Other payment`. |
| `a85636c1` | `formatMoneyAmount()` in `moneyPosting.ts`; 13 sites in `RechargeRepository` | Stored summaries said `700579 LBP` beside a badge saying `700,579 LBP`. Stored-once strings: **historical rows keep the raw form** (deliberate, no migration). |
| `7cd293d8` | New bill appears in Settle list immediately | 30s `staleTime` + zero invalidation from bill creation + **Refresh button didn't refetch `unsettledQuery`**. Fixed both. Found because lira-141 passes alone but failed in a full run (needs an accumulated DB). |
| in `fb44b9ac` | **Cash-in leak**: `createTopUpFromDrawer` legs `TOPUP_METHOD`("CASH") → `DRAWER_TRANSFER_METHOD` | A $100 internal drawer move counted as $100 *customer cash-in* in `getCashFlowByDate` (proven failing-first: Expected 0, Received 100). External Cash-In (`createTopUp`) is untouched and MUST keep counting. |
| in `fb44b9ac` | General↔PCD routed through `transferBetweenDrawers` **both directions** (`DrawerTopUpModal.tsx`) | Both directions now symmetric, both reversible (old path had no Void/Refund — raw `UPDATE`, no source leg; `DRAWER_TOPUP` stays in `NON_REVERSIBLE_TRANSACTION_TYPES`). Non-primary source drawers still use `createFromDrawer` **deliberately** — guarded by `DrawerTopUpModal.fromDrawerRouting.test.tsx`. |
| in `fb44b9ac` | `PRIMARY_CASH_DRAWER_NAMES` now exported from core's `browser.ts`; frontend mirror deleted | Rule 14. Don't re-introduce a local copy. |

## 2. Owner decisions made this session (do not relitigate)

- **Top-up vs Other payment**: commission is a separate payment regardless of
  who owes whom; Top-up = provider pays us into our provider drawer.
- **Arrows rule (b)**: badge follows what actually moved, two arrows for
  drawer↔drawer.
- **Method column `—` on internal drawer moves: KEEP the dash** (offered an
  "Internal transfer" label; owner chose the dash).
- **Lump sum vs Rate×count: leave as-is** (option A).
- **Settle-list staleness: fix both causes** (done).
- **v1.30.2 released before local verification at owner's explicit
  instruction** — verification then came back green everywhere.
- Owner **confirmed the drawer-move fix live** (03:14 rows, both directions
  symmetric); the "Cash disappeared from Method column" is the fix, not a bug.

## 3. Open items, in the order I'd take them

1. **ASK OWNER — historical `method='CASH'` rows.** Every drawer-to-drawer
   top-up recorded before v1.30.2 still inflates past-dated
   `getCashFlowByDate` totals. Offer to MEASURE the scale first (read the live
   DB copy with Python stdlib sqlite3 — see MEMORY), then owner decides on a
   backfill. Never rewrite payments rows without explicit approval.
2. **LIRA-114** — FOR-partner service on Debt appears to move General.
   Owner-reported + money-relevant. Top of the NEEDS-INTERVIEW list.
3. Remaining NEEDS-INTERVIEW: LIRA-058 (OMT App top-up design), LIRA-079
   (refund scope/Void button), LIRA-088 (MTC/Alfa provider-balance decrement),
   LIRA-096 (remove "Record Transaction" from Partners).
4. **LIRA-138** — generalise commission drawer top-up to OMT/WHISH (designed,
   not built). `isSupplierCredit`'s amber marker is the ready-made hook.
5. **LIRA-139** — sort-by-Amount ignores `amount_lbp` (needs owner semantics
   decision); **LIRA-140** — non-till money renders like till cash.
6. Small debt: session-cart bill path doesn't invalidate the unsettled query
   (`SessionCheckoutModal` is cross-module — needs a decision, see 7cd293d8's
   commit message); Method column `—` on Top-up settlements (fix = branch the
   Method `<td>`, NOT `methodLegsFor` — that drives sort + CSV export);
   `Dashboard.tsx:218` duplicates `primaryCashDrawerName()` (rule-14 cleanup).
7. **Web e2e was NOT run this session** (`yarn test:e2e:web`). Frontend
   changes are dual-transport-safe by construction (adapter reuse, REST routes
   pre-existed) but unproven in web mode. If you run it: it flips the ABI —
   run desktop e2e FIRST or plan a `yarn dev` cycle after (MEMORY).

## 4. Fresh traps (beyond CLAUDE.md / MEMORY)

- **Owner works in the live app in parallel sessions.** Twice this session a
  jest/rebuild run failed with `EBUSY` because the app held
  `better_sqlite3.node`, and 5 electron processes + port 5173 appeared
  mid-task. Probe before native work:
  `Get-Process electron`; `Get-NetTCPConnection -LocalPort 5173 -State Listen`.
  **Do not kill their session without asking** (this time they said "u close
  the app" explicitly first). One agent side-stepped a locked module by
  building an isolated Node-ABI copy under `packages/core/node_modules/` and
  deleting it after — acceptable pattern, never touch the root copy.
- **Disk: C: hit 0 bytes free this session** and broke Write/Bash/Playwright
  in confusing ways. ~2.6 GB free at session end — still fragile. Playwright
  leaks `%TEMP%\liratek-e2e-userdata-*` (~35MB/run); cleanup recipe is in
  MEMORY (`e2e-temp-profile-leak-and-full-disk`). Check `(Get-PSDrive C).Free`
  FIRST when tools fail weirdly.
- **`yarn release` (`scripts/release.cjs`) does `git add -A` → commit → tag →
  push** and CI publishes a Windows build. Any untracked file in the tree gets
  swept into the release commit. Keep the tree clean; `build.yml` DOES run
  `yarn test` on the tag (line ~132), so CI is a real safety net.
- **Working e2e invocation** (agent shells):
  `cd frontend && env -u ELECTRON_RUN_AS_NODE npx playwright test --config playwright.electron.config.ts --reporter=list`
  `yarn rebuild:native` + the must-THROW probe restored the Electron ABI
  reliably this session (no `yarn dev` cycle was needed).
- **lira-141 is order-sensitive by design**: it passed in isolation while a
  real staleness bug only reproduced in the full suite. Never accept an
  isolated pass as proof for that spec.

## 5. Working agreement (owner-set, standing)

- Opus orchestrates; **ALL code edits go to Sonnet agents** (`model:
  'sonnet'`). Orchestrator designs, reviews diffs itself, verifies, reports.
- Verify agents' load-bearing claims yourself (this session: an agent
  reported the ABI state backwards; another's "CI e2e is vacuous" claim from a
  past session was wrong).
- Rule 17 failing-first is enforced — demand the actual pre-fix failure
  output in reports.
- Never revert/clean uncommitted diffs you didn't write — parallel-session
  owner edits (MEMORY: destroyed owner work once on 2026-08-09).
- Reading the live DB (`~/Documents/LiraTek/liratek.db`) is authorized;
  copy `.db` + `-wal` + `-shm`, query the copy with Python stdlib sqlite3.
