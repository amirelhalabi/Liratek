# LiraTek — Left To Do

Working list from the notes review. **Every item must be validated by an e2e spec** in
`frontend/tests/e2e-electron/lira-*.spec.ts` before it's considered done.

## How to run e2e (required sequence)

1. `yarn dev` and wait for it to finish starting (rebuilds `better-sqlite3` to the Electron ABI + builds `electron-app/dist`).
2. **Stop** `yarn dev` (frees port 5173 + the Electron instance).
3. `yarn test:e2e`.

After editing `packages/core/**`: `cd packages/core && npm run build` then sync `dist` into
`node_modules/@liratek/core/dist` (Windows: `xcopy /e /y /q`; macOS: `rsync -a --delete packages/core/dist/ node_modules/@liratek/core/dist/`), or the runtime runs stale code.

## E2E assertion discipline (shared accumulating DB — CLAUDE.md rule 15)

- Match the row by **identity** (type + provider/`service_type`, `source_id`, `item_key`, or a unique amount/label) — never `getRecent(...)[0]` or `tbody tr.first()`.
- Assert **deltas**: snapshot the drawer/ledger/balance immediately before the action, compare after. Never absolute totals or row position.
- One action can write **multiple** unified-transaction rows (e.g. a `FINANCIAL_SERVICE` + an auto `SUPPLIER_PAYMENT` sibling); `created_at` is second-granular so same-second rows tie.
- IPC-driven specs (`appPage.evaluate(() => window.api.*)`) are preferred for money-flow correctness; DOM specs for UI-visibility items.

---

## ✅ Done

All planned items are complete and e2e-validated. Cluster order was C1→C5, then A/B items, then D1.

| Item | Fix | Validation |
| --- | --- | --- |
| **C1** — OMT/WHISH system RECEIVE split-currency cashout drops the LBP leg | `FinancialServiceRepository.ts` RECEIVE CASH cashout deducts each **IN** payout leg in its own currency (OUT/change legs stay with the shared return-leg loop — a first pass wrongly spread `...returnLegs` into the payout loop, double-debiting change legs; fixed). | Unit: `FinancialServiceRepository.receiveSplitPayout.test.ts` (3 cases incl. an OUT-leg no-double-debit guard, verified to fail on the buggy code). **E2E: `lira-074-omt-receive-split-payout.spec.ts`** (3 cases) — split payout deducts General USD −190 **and** LBP −540,000; single-currency unchanged; OUT leg debited exactly once. *(e2e passed)* |
| **C2** — OMT/WHISH in/out semantics | Badge: `getCashFlowDirection` (extracted to `features/audit/cashFlow.ts`) branches on `metadata.service_type` — SEND/BILL → in, RECEIVE → out; badge got `data-testid="cash-flow-badge"` + `data-direction`. Legs: `RESERVE` added to `INTERNAL_LEG_METHODS` in `TransactionRepository` — the SEND cash-reserve settlement leg leaked into the row as a bogus "out: $X" (caught by the first lira-075 run). Rate: `useSellRate()` now also returns `buyRate`; `OmtWhishAppTransferForm` picks buy rate for RECEIVE (sell for SEND). | Unit: `frontend/.../audit/__tests__/cashFlow.test.ts` + RESERVE case in `TransactionRepository.paymentLegs.test.ts` — both verified to fail on pre-fix code. **E2E: `lira-075-omt-inout-semantics.spec.ts`** — SEND row badge=in, legs "in:" only; RECEIVE row badge=out, legs "out:" only with BOTH payout currencies. *(e2e passed)* |
| **C3** — Supplier ledger = transaction amount | `FinancialServiceRepository` auto-ledger: `ledgerAmount = \|amount\|` — dropped the `+ omtFee` (SEND) and `+ commission` (RECEIVE) add-ons that booked the customer-paid total and left a phantom residue after settlement (Settle tab nets `owed − commission = amount`). Cost/price flow still books `cost`. No migration for historical entries. | Unit: `FinancialServiceRepository.supplierLedgerAmount.test.ts` (3 cases incl. split-pay-different-total; all 3 verified to fail on pre-fix code). **E2E: `lira-076-supplier-ledger-amount.spec.ts`** — SEND split-pay delta +80 exactly (not +85/paid total); RECEIVE delta −40 exactly (not −40.40). *(e2e passed)* |
| **C4** — App drawers not moving on send/receive | `FinancialServiceRepository`: OMT_APP/WHISH_APP transfers (no cost/price) now route through the Binance wallet-transfer block (gate `isBINANCE \|\| isAppWallet`, wallet currency = service currency). SEND: app drawer −amount, cash +(amount+fee); RECEIVE: app drawer +amount, cash −(amount−fee). Commission-inflow block skips app wallets (fee realized in the spread). App-wallet legs (`OMT_APP`/`WHISH_APP` methods) filtered from the audit legs summary like the USDT leg. | Unit: `FinancialServiceRepository.appWalletTransfer.test.ts` (5 cases incl. Binance control; 4 app cases verified to fail on the pre-fix gate). **E2E: `lira-077-app-drawer-movement.spec.ts`** — OMT_APP SEND/RECEIVE ∓20 deltas on OMT_App & General, WHISH_APP SEND, Binance USDT control. *(e2e passed)* |
| **C5** — Prepaid-units redesign | Supplier debt booked ONCE at top-up (`topUpFromSupplier` → TOP_UP entry + drawer credit); cost/price sales book **NO per-sale SALE_COST** — drawer draw-down only. Loto untouched (books its own ledger — exception holds). Migration **v115** adds `financial_services.supplier_debt_booked` (backfilled 1 for legacy cost-flow SENDs); `getUnsettledBySupplier` only offers LEGACY rows for per-transaction settle. `create_db.sql` updated. Katsh bill verified correct already (General +price, Katsh −cost, no visible "out" leg). `lira-061` e2e rewritten to the C5 contract (was asserting the superseded per-sale SALE_COST model). | Unit: `FinancialServiceRepository.saleCost.test.ts` rewritten to the C5 contract (5 cases verified to fail on the reintroduced pre-C5 booking). **E2E: `lira-078-prepaid-units.spec.ts`** — top-up +40/+40; sale −9 drawer / supplier unchanged; loto exception holds; cash bill General +150k, in-only legs. Rewritten `lira-061` passes. *(e2e passed)* |
| **A2 + A3** — Per-mode party fields + client in the summary | A2: `Services/index.tsx` and `OmtWhishAppTransferForm.tsx` render only the mode's own party (SEND → sender pair, RECEIVE → receiver pair) and CLEAR the hidden pair on mode switch. Binance (`CryptoForm`) already has a single client pair — n/a. A3: system form gets an inline client strip (`data-testid="client-summary"`); app + crypto forms prepend Client/Phone rows to their PaymentSheet `summary`. (Sheet rows not DOM-asserted — covered by the shared code path + the system form's asserted strip.) | **E2E: `lira-083-send-receive-fields.spec.ts`** — SEND↔RECEIVE visibility toggles on both forms; summary strip shows name+phone; switching back hides AND clears. *(e2e passed)* |
| **A4 + B1** — Setup checkpoint + per-currency starting amounts | A4: `StepComplete.tsx` ALWAYS writes the initial checkpoint on setup completion (was skipped when no amounts entered). B1: verified already implemented end-to-end (per-drawer × per-currency → `createCheckpoint` reconciles `drawer_balances`); inputs got `data-testid="setup-amount-<drawer>-<currency>"`. E2E fixtures seed General USD=500 / LBP=9,000,000 in step 6 and validate `dashboard.getDrawerBalances()` right after setup on EVERY worker. | **E2E: `lira-085-setup-checkpoint.spec.ts`** — setup checkpoint exists, is the timeline's FIRST entry, and records the exact per-currency amounts. *(e2e passed — full suite green with the new baseline)* |
| **A6** — Mixed `created_at` formats break ordering | `SupplierRepository.settleTransactions` + `recordSupplierCashflow` stamp with SQLite `datetime('now')` instead of JS `toISOString()` (ISO's `'T'` string-sorts above every space-format row of the same day, pinning settlement rows to the top). Migration **v116** normalizes historical ISO rows (`transactions.created_at`, `supplier_ledger.created_at`, `financial_services.settled_at`). | Unit: format-regex case in `SupplierRepository.settlement.test.ts` (verified to fail with an ISO stamp). **E2E: `lira-079-created-at-ordering.spec.ts`** — supplier row stamps SQLite-format; a newer SEND sorts ABOVE it. *(e2e passed)* |
| **A7** — Recurring silent e2e death ("Target page closed") | RESOLVED: parallel Electron boot race. app.spec solo passes 14/14 with a clean log; every death occurred only in multi-worker runs, hitting whichever test ran ~5–15s into worker 0 — the sibling-boot window. Fix: fixtures stagger worker boots (`WORKER_INDEX * 2000ms`); the `NODE_ENV=test` uncaughtException guard in `main.ts` stays as defense-in-depth. Also fixed: `SessionContext` polls active sessions every 7s (a session closed via IPC left a stale floating window swallowing clicks). | Subsequent full parallel runs: zero silent deaths. |
| **B2** — Debts Excel import inflation | Root cause: NO dedup — re-importing the same file re-inserted every entry for existing clients, multiplying totals. Fix: `DebtRepository.countIdenticalRawEntries` + per-identity-key skip-budget in `ClientService.importClientsWithDebts` (MULTISET semantics — a file with 2× identical Alfa 600,000 LBP rows imports both first time; re-imports skip exactly what exists; partial re-imports import only the surplus). `duplicatesSkipped` in result/alert. Bonus: `insertRawEntry` normalizes ISO dates. | Unit: `ClientService.importDebts.test.ts` (6 cases; dedup + format proven to fail with the fixes disabled). **E2E: `lira-080-debt-import-totals.spec.ts`** — exact fixture totals; re-import changes nothing. *(e2e passed)* |
| **B3** — Maintenance via customer account → Debts | NOT REPRODUCIBLE: `processPayments` already books the shortfall (incl. full CUSTOMER_ACCOUNT) as `Maintenance Debt`. Locked in with tests instead of a fix. If the symptom recurs live, capture the exact UI steps. | Unit: `MaintenanceService.customerAccount.test.ts` (full account / split / cash-only). **E2E: `lira-081-maintenance-customer-account.spec.ts`** — client debt +45 delta. *(e2e passed)* |
| **B4** — Supplier opening balance (both directions) | Backend already supported signed `ADJUSTMENT` entries; gap was UX. Settings → Supplier Ledger manual entry: direction toggle for ADJUSTMENT ("We owe supplier" / "Supplier owes us") signs the amounts. Setup-wizard entry deferred (no suppliers step exists — new-feature territory). | Unit: 3 cases in `SupplierRepository.settlement.test.ts`. **E2E: `lira-084-supplier-opening-balance.spec.ts`** — ±deltas on `suppliers.getBalances`. *(e2e passed)* |
| **B5** — Profits: maintenance + recharge teshriji | Maintenance profit was ALWAYS zero: `ProfitRepository` filtered on a fictional `'completed'` status the workflow never produces. Fixed with ONE named fragment `MAINTENANCE_COMPLETED = "m.status IN ('Delivered','Delivered_Paid')"` (rule 14). Teshriji (CREDIT_TRANSFER) verified covered. The old unit test seeded the fictional status — exactly how the bug survived; now seeds real statuses. | Unit: `ProfitService.transactionBased.test.ts` (Delivered_Paid case fails on the old predicate; In_Progress excluded; teshriji counted). **E2E: `lira-086-profits-coverage.spec.ts`** — maintenance +35 exactly; teshriji +0.34 net of SMS. *(e2e passed)* |
| **B6** — Transactions: cash grouping (+ SUPPLIER_CREDIT decision) | Owner decisions: Supplier Credit rows STAY visible (real commission revenue); grouping = **"Cash only (till)"** filter. `isCashTransaction` predicate in `cashFlow.ts` (row touched the till iff it has a CASH leg), `cash_only` FilterOption, client-side filter in `TransactionsViewer.load()`. | Unit: predicate cases in `cashFlow.test.ts`. **E2E: extended `lira-transactions-hidden-types.spec.ts`** — cash row visible, wallet-only row filtered out. *(e2e passed)* |
| **B7** — Loto in/out not shown in transactions table | `getCashFlowDirection` had no `LOTO`/`LOTO_CASH_PRIZE` case → blank badge on every loto row. Added: `LOTO` → in, `LOTO_CASH_PRIZE` → out. Legs subtext already worked. | Unit: 2 cases (fail unmapped). **E2E: `lira-082-loto-inout.spec.ts`** — structured "in" leg + rendered badge. *(e2e passed)* |
| **D1** — Currency in/out by date report (Cash Report) | Owner decisions: toolbar button on the Transactions page → modal, daily rows (Date × USD in/out × LBP in/out + totals footer), customer-cash-only legs, export via DataTable's built-in Excel/PDF. Backend: `TransactionRepository.getCashFlowByDate` buckets by `created_at` (which IS the business date — `createTransaction` coalesces `transaction_time` into it; a phantom-column assumption was caught by the owner's live test and fixed). Customer-cash rule is ONE definition: `isInternalLegJs` + `customerCashLegSql` (SQL mirror from the same constant sets, rule 14). | Unit: 4 cases in `TransactionRepository.paymentLegs.test.ts` (exact sums, internal exclusion, business-date bucketing, VOIDED excluded). **E2E: `lira-087-currency-by-date.spec.ts`** — backdated known txns; IPC aggregation exact AND the modal renders the row. *(e2e passed)* |
| Maintenance delete semantics (owner feedback 2026-07-03) | Deleting a job no longer voids its transaction or emits a −amount reversal. UNPAID jobs: pure status change (`Deleted`, hidden from lists). PAID jobs (any ACTIVE unified transaction — cash, debt, or session-linked): BLOCKED with "refund or void it instead". UI surfaces the block. | Unit: 2 cases (both fail on the old void-path). **E2E: extended `lira-081`** — draft deletes cleanly; paid delete blocked. *(e2e passed)* |
| Wider scrollbars / checkpoint popup scroll / expenses scroll / shared rate hook | `index.css` 4px→10px; `CheckpointTimeline` `max-h-[90vh] overflow-y-auto`; `Expenses` flex chain; `useSellRate()` consolidation + `EXCHANGE_RATE` const | Visual/manual + rate-dependent specs |

---

## ⏳ Still waiting — blocked on outside input

### A1 — Card labels by credit amount (bills #2)
Cards show `8.xx` (the price) instead of the credit amount.
**Blocked on: the correct credit-denomination values for the Alfa/MTC cards (owner to provide).**
- Where: `frontend/src/data/mobileServices.ts`, Recharge card grid (`CardGridPayView.tsx`, `TelecomForm.tsx`).
- **E2E `lira-0xx-card-labels`:** open Recharge → Alfa/MTC, assert each card button label reads the credit amount and is **not** the `$8.xx` price.

### A5 — Services page keyboard/description bug (#6)
Symptom: name/phone/note don't accept keyboard input; "Please enter a service description" fires.
**Blocked on: a live repro** (which screen, which inputs, what was open) to confirm root cause (SearchBar-as-description vs. a focus/overlay issue).
- Where: `features/custom-services/pages/CustomServices/index.tsx`.
- **E2E `lira-0xx-services-input`:** `fill` the customer name/phone/note inputs, assert `toHaveValue`; enter a description, submit, assert success (no validation alert).

### B3 (follow-up) — Maintenance customer-account symptom, if it recurs
Backend + e2e prove the debt IS booked today. **Blocked on: a live repro** of the originally-reported symptom — if it happens again, capture the exact steps/screens so the UI path can be chased.

### B6b — Loto supplier ledger uses an inverted sign convention (found during C5 e2e)
`LotoTicketRepository.createTicket` books the shop's liability as a NEGATIVE `PAYMENT` entry (−(sale−commission)); every other supplier books debt as POSITIVE. The Suppliers page sums ledger rows, so LOTO reads inverted vs all other suppliers. Loto settlement flows are built around this convention.
**Blocked on: scheduling — a coordinated redesign** (repo + settlement + Suppliers display + migration for historical rows), not a drive-by fix.

### C4/C5 (deferred slice) — App-transfer provider-fee split
Fee semantics for app transfers kept Binance-style (fee = commission, realized in the wallet/cash spread); the provider's share of `omtFee`/`whishFee` is NOT yet split out to the supplier ledger. Historical SALE_COST entries untouched (settle/pay down as before).
**Blocked on: product decision** — how the provider's fee share should be reconciled (per-transfer supplier debt vs. settlement-time adjustment).
